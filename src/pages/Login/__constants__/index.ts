const enum LoginSms {
  title = 'Введите код SMS:',
  placeholder = 'SMS code',
}

const enum LoginEmail {
  title = 'Введите код Email:',
  placeholder = 'Email code',
}

export { LoginSms, LoginEmail };
